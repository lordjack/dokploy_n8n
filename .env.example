# Variáveis de ambiente necessárias para o Dokploy no Render.com
# Configure estas variáveis no painel do Render.com

# Porta que o serviço irá usar (definida automaticamente pelo Render)
PORT=3000

# Host para bind do servidor
DOKPLOY_HOST=0.0.0.0

# Chave secreta para segurança (OBRIGATÓRIO - gere uma chave forte)
# Use: openssl rand -hex 32
DOKPLOY_SECRET_KEY=your_secret_key_here_replace_with_actual_secret

# Configurações opcionais do Docker
DOCKER_HOST=unix:///var/run/docker.sock

# Configurações de banco de dados (se necessário)
# DATABASE_URL=postgresql://user:password@host:port/database

# Domínio personalizado (se configurado no Render)
# DOKPLOY_DOMAIN=your-app-name.onrender.com

# Configurações de email (se necessário para notificações)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Configurações de SSL/TLS
DOKPLOY_SSL=true

# Configurações de log
LOG_LEVEL=info